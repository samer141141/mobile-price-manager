# Mobile Price Manager
Next.js + Supabase inventory and profit manager for mobile phone resale.

## Setup
1. Copy `.env.example` to `.env.local`.
2. Add your Supabase URL and publishable key.
3. Run `npm install` then `npm run dev`.

The Supabase `phones` table must exist as created during setup.
