import './globals.css'
export const metadata={title:'Mobile Price Manager',description:'Phone inventory, pricing and profit manager'}
export default function RootLayout({children}){return <html lang="en"><body>{children}</body></html>}
